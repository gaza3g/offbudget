$("#item-list-<%=@budget.id%>").html("<%= escape_javascript(render 'shared/item_info', :budget => @budget ) %>")
$("#notice").html('<div class="flash success"><%= escape_javascript(flash.delete(:success)) %></div>');

$('.item-edit').editable('/item/update', {
    name		:	$(this).attr('name'),
    id			:	$(this).attr('id'),
    cancel		:	'Cancel',
    submit		:	'OK',
    indicator	:	'Saving...',
    tooltip		:	'Click to edit...',
	method		: 	'put'
  });