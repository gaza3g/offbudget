<%= form_for @budget do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
	<h6>Create a new budget: </h6>
    <%= f.text_field :name %> 
	<%= f.submit "Create" %>
  </div>
<% end %>
